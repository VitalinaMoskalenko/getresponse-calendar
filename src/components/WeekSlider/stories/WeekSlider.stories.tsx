import React from "react";
import styled from "styled-components";
import WeekSlider from "..";

export default { title: "WeekSlider" };

export const defaultWeekSlider = () => <WeekSlider countWeek={12} />;
